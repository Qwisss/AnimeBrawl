using UnityEngine;
public class TriggerAttack : MonoBehaviour
{
/*
    [Header("Components")]
    [SerializeField] private PlayerLocomotion _playerLocomotion;

    private void Start()
    {
        Iniatialize();
    }

    private void Iniatialize()
    {
        if (_playerLocomotion == null)
        {
            _playerLocomotion = GetComponentInParent<PlayerLocomotion>();
        }
    }
    
    private void OnTriggerEnter(Collider other)
    {
        print("Trigger Enter");
        Enemy target = other.GetComponent<Enemy>();
        if (target != null)
        {
            if (!_playerLocomotion.targets.Contains(target))
            {
                _playerLocomotion.targets.Add(target);
            }
        }
    }

    private void OnTriggerExit(Collider other)
    {
        print("Trigger Exit");
        Enemy target = other.GetComponent<Enemy>();
        if (target != null)
        {
            _playerLocomotion.targets.Remove(target);
        }

    }*/
}